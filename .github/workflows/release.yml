name: Release (tag)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure (MSVC x64)
        run: >-
          cmake -S . -B build
          -A x64
          -DMIC_TOGGLE_VERSION="${{ github.ref_name }}"

      - name: Build (Release)
        run: cmake --build build --config Release --parallel

      - name: Prepare artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          Copy-Item -Force build\Release\mic_toggle.exe dist\MicToggle.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/MicToggle.exe
